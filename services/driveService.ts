// Google Drive Service Disabled

export const initGoogleDrive = (callback: (token: string) => void) => {
    // Disabled
};

export const requestAccessToken = () => {
    // Disabled
};

export const uploadFileToDrive = async (
    title: string, 
    content: string, 
    token: string
): Promise<any> => {
    return Promise.resolve({});
};